sudo: required
dist: trusty

language: c
compiler:
    - gcc
    - clang

before_install:
    - sudo apt-get -y install check

before_script:
    - mkdir build
    - cd build
    - cmake ..

script: make && make test
